using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DataStructComponent : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        // 기본 자료형
        int intValue = 0; //자료형 -21억 ~ 21억까지 표현 가능
        uint uintValue = 0; //자료형 0 ~ 42억까지 표현 가능
        Int64 longValue = 0; // 자료형 
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
